{
  "python": {
    "version": "3.11",
    "entry": "app/main.py",
    "package_manager": "pip",
    "install_cmd": "pip install -r requirements.txt",
    "build_cmd": "",
    "start_cmd": "alembic upgrade head && uvicorn app.main:app --host 0.0.0.0 --port ${PORT}"
  }
}
